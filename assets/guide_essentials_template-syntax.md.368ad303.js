import{c as e,d as s,t as a,a as n,e as o,o as t}from"./app.299c3ad9.js";const l=n('<div class="warning custom-block"><p class="custom-block-title">현재 이 문서는 번역 작업이 진행중입니다</p></div><h1 id="template-syntax" tabindex="-1">Template Syntax <a class="header-anchor" href="#template-syntax" aria-hidden="true">#</a></h1><h1 id="템플릿-문법" tabindex="-1">템플릿 문법 <a class="header-anchor" href="#템플릿-문법" aria-hidden="true">#</a></h1><p>Vue uses an HTML-based template syntax that allows you to declaratively bind the rendered DOM to the underlying component instance&#39;s data. All Vue templates are syntactically valid HTML that can be parsed by spec-compliant browsers and HTML parsers.</p><p>Vue는 컴포넌트 인스턴스의 데이터를 DOM에 선언적으로 바인딩할수 있는 HTML 기반의 템플릿 문법을 사용합니다. 모든 Vue 템플릿은 표준을 준수하는 브라우저와 HTML 파서에서 파싱될수 있는 문법적으로 유효한 HTML 입니다.</p><p>Under the hood, Vue compiles the templates into highly-optimized JavaScript code. Combined with the reactivity system, Vue is able to intelligently figure out the minimal number of components to re-render and apply the minimal amount of DOM manipulations when the app state changes.</p><p>내부적으로 Vue는 템플릿을 고도로 최적화된 JavaScript 코드로 컴파일합니다. 반응성 시스템과 결합된 Vue는 앱 상태가 변경될 때 다시 렌더링할 컴포넌트의 최소 수를 지능적으로 파악하여, 최소한의 DOM 조작을 적용할 수 있습니다.</p><p>If you are familiar with Virtual DOM concepts and prefer the raw power of JavaScript, you can also <a href="/guide/extras/render-function.html">directly write render functions</a> instead of templates, with optional JSX support. However, do note that they do not enjoy the same level of compile-time optimizations as templates.</p><p>Virtual DOM 개념에 익숙하고 JavaScript의 원시 기능을 선호하는 경우 템플릿 대신 JSX 지원 옵션을 사용하여 <a href="/guide/extras/render-function.html">렌더링 기능을 직접 작성</a>할 수도 있습니다. 그러나 템플릿과 동일한 수준의 컴파일 타임 최적화를 제공하지는 않습니다.</p><h2 id="text-interpolation" tabindex="-1">Text Interpolation <a class="header-anchor" href="#text-interpolation" aria-hidden="true">#</a></h2><h2 id="텍스트-보간-text-interpolation" tabindex="-1">텍스트 보간(Text Interpolation) <a class="header-anchor" href="#텍스트-보간-text-interpolation" aria-hidden="true">#</a></h2><p>The most basic form of data binding is text interpolation using the &quot;Mustache&quot; syntax (double curly braces):</p><p>데이터 바인딩의 가장 기본적인 형태는 콧수염 표기법이라고 알려진 &quot;Mustache&quot; 구문(이중 중괄호)을 사용하는 텍스트 보간입니다.</p><div class="language-vue-html"><pre><code><span class="line"><span style="color:#89DDFF;">&lt;</span><span style="color:#F07178;">span</span><span style="color:#89DDFF;">&gt;</span><span style="color:#A6ACCD;">Message: </span><span style="color:#89DDFF;">{{</span><span style="color:#A6ACCD;"> msg </span><span style="color:#89DDFF;">}}&lt;/</span><span style="color:#F07178;">span</span><span style="color:#89DDFF;">&gt;</span></span>\n<span class="line"></span></code></pre></div><p>The mustache tag will be replaced with the value of the <code>msg</code> property from the corresponding component instance. It will also be updated whenever the <code>msg</code> property changes.</p><p>Mustache 태그는 해당 컴포넌트 인스턴스의 <code>msg</code> 속성 값으로 대체됩니다. 그리고 <code>msg</code> 속성이 변경될 때마다 업데이트됩니다.</p><h2 id="raw-html" tabindex="-1">Raw HTML <a class="header-anchor" href="#raw-html" aria-hidden="true">#</a></h2><p>The double mustaches interprets the data as plain text, not HTML. In order to output real HTML, you will need to use the <a href="/api/built-in-directives.html#v-html"><code>v-html</code> directive</a>:</p><p>mustache은 데이터를 HTML이 아닌 일반 텍스트로 해석합니다. 실제 HTML을 출력하려면 <a href="/api/built-in-directives.html#v-html"><code>v-html</code> 디렉티브</a>을 사용해야 합니다.</p><div class="language-vue-html"><pre><code><span class="line"><span style="color:#89DDFF;">&lt;</span><span style="color:#F07178;">p</span><span style="color:#89DDFF;">&gt;</span><span style="color:#A6ACCD;">문자열 보간: </span><span style="color:#89DDFF;">{{</span><span style="color:#A6ACCD;"> rawHtml </span><span style="color:#89DDFF;">}}&lt;/</span><span style="color:#F07178;">p</span><span style="color:#89DDFF;">&gt;</span></span>\n<span class="line"><span style="color:#89DDFF;">&lt;</span><span style="color:#F07178;">p</span><span style="color:#89DDFF;">&gt;</span><span style="color:#A6ACCD;">v-html 디렉티브: </span><span style="color:#89DDFF;">&lt;</span><span style="color:#F07178;">span</span><span style="color:#89DDFF;"> </span><span style="color:#C792EA;">v-html</span><span style="color:#89DDFF;">=</span><span style="color:#89DDFF;">&quot;</span><span style="color:#A6ACCD;">rawHtml</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">&gt;&lt;/</span><span style="color:#F07178;">span</span><span style="color:#89DDFF;">&gt;&lt;/</span><span style="color:#F07178;">p</span><span style="color:#89DDFF;">&gt;</span></span>\n<span class="line"></span></code></pre></div>',20),p=o("v-html 디렉티브: "),r=n('<p>Here we&#39;re encountering something new. The <code>v-html</code> attribute you&#39;re seeing is called a <strong>directive</strong>. Directives are prefixed with <code>v-</code> to indicate that they are special attributes provided by Vue, and as you may have guessed, they apply special reactive behavior to the rendered DOM. Here, we&#39;re basically saying &quot;keep this element&#39;s inner HTML up-to-date with the <code>rawHtml</code> property on the current active instance.&quot;</p><p>지금 보시는 <code>v-html</code> 같은 속성을 <strong>디렉티브</strong>(directive)이라고 합니다. 디렉티브는 Vue에서 제공하는 특수 속성임을 나타내는 접두사 <code>v-</code>를 사용하며, 예상하신대로 렌더링된 DOM에 특별한 반응형 행위를 적용합니다. 위의 코드에서는 &quot;현재 활성 인스턴스의 <code>rawHtml</code> 속성을 사용하여 이 엘리먼트의 <code>innerHtml</code>을 최신 상태로 유지합니다.&quot;라는 행위를 수행합니다.</p><p>The contents of the <code>span</code> will be replaced with the value of the <code>rawHtml</code> property, interpreted as plain HTML - data bindings are ignored. Note that you cannot use <code>v-html</code> to compose template partials, because Vue is not a string-based templating engine. Instead, components are preferred as the fundamental unit for UI reuse and composition.</p><p><code>rawHtml</code> 속성에 저장된 값은 plain HTML로 해석되어 <code>span</code>의 내용(<code>innerHTML</code>)을 대체 하게 됩니다. 데이터 바인딩은 무시됩니다. Vue는 문자열 기반 템플릿 엔진이 아니기 때문에 <code>v-html</code>을 사용하여 템플릿 조각을 작성할 수는 없습니다. UI 재사용 및 구성을 위한 기본 단위로 컴포넌트가 선호됩니다.</p><div class="warning custom-block"><p class="custom-block-title">Security Warning</p><p>Dynamically rendering arbitrary HTML on your website can be very dangerous because it can easily lead to <a href="https://en.wikipedia.org/wiki/Cross-site_scripting" target="_blank" rel="noopener noreferrer">XSS vulnerabilities</a>. Only use <code>v-html</code> on trusted content and <strong>never</strong> on user-provided content.</p></div><div class="warning custom-block"><p class="custom-block-title">보안 경고</p><p>웹사이트에서 임의의 HTML을 동적으로 렌더링하는 것은 <a href="https://en.wikipedia.org/wiki/Cross-site_scripting" target="_blank" rel="noopener noreferrer">XSS 취약점</a>으로 쉽게 이어질 수 있기 때문에 매우 위험할 수 있습니다. 신뢰할 수 있는 콘텐츠에만 <code>v-html</code>을 사용하고 사용자가 제공한 콘텐츠에는 <strong>절대</strong> 사용하지 마세요.</p></div><h2 id="attribute-bindings" tabindex="-1">Attribute Bindings <a class="header-anchor" href="#attribute-bindings" aria-hidden="true">#</a></h2><h2 id="속성-바인딩" tabindex="-1">속성 바인딩 <a class="header-anchor" href="#속성-바인딩" aria-hidden="true">#</a></h2><p>Mustaches cannot be used inside HTML attributes. Instead, use a <a href="/api/built-in-directives.html#v-bind"><code>v-bind</code> directive</a>:</p><p>Mustache 표기법은 HTML 속성에는 사용 할 수 없습니다. 대신 <a href="/api/built-in-directives.html#v-bind"><code>v-bind</code> 디렉티브</a>를 사용합니다:</p><div class="language-vue-html"><pre><code><span class="line"><span style="color:#89DDFF;">&lt;</span><span style="color:#F07178;">div</span><span style="color:#89DDFF;"> </span><span style="color:#C792EA;">v-bind</span><span style="color:#89DDFF;">:</span><span style="color:#C792EA;">id</span><span style="color:#89DDFF;">=</span><span style="color:#89DDFF;">&quot;</span><span style="color:#A6ACCD;">dynamicId</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">&gt;&lt;/</span><span style="color:#F07178;">div</span><span style="color:#89DDFF;">&gt;</span></span>\n<span class="line"></span></code></pre></div><p>The <code>v-bind</code> directive instructs Vue to keep the element&#39;s <code>id</code> attribute in sync with the component&#39;s <code>dynamicId</code> property. If the bound value is <code>null</code> or <code>undefined</code>, then the attribute will be removed from the rendered element.</p><p><code>v-bind</code> 디렉티브는 엘리먼트의 <code>id</code> 속성을 컴포넌트의 <code>dynamicId</code> 속성과 동기화된 상태로 유지하도록 Vue에 지시합니다. 바인딩된 값이 <code>null</code> 또는 <code>undefined</code>이면 속성이 렌더링된 엘리먼트에서 제거됩니다.</p><h3 id="shorthand" tabindex="-1">Shorthand <a class="header-anchor" href="#shorthand" aria-hidden="true">#</a></h3><h3 id="단축-표기법" tabindex="-1">단축 표기법 <a class="header-anchor" href="#단축-표기법" aria-hidden="true">#</a></h3><p>Because <code>v-bind</code> is so commonly used, it has a dedicated shorthand syntax:</p><p><code>v-bind</code>는 매우 자주 사용되기 때문에 전용 단축 구문이 있습니다:</p><div class="language-vue-html"><pre><code><span class="line"><span style="color:#89DDFF;">&lt;</span><span style="color:#F07178;">div</span><span style="color:#89DDFF;"> :</span><span style="color:#C792EA;">id</span><span style="color:#89DDFF;">=</span><span style="color:#89DDFF;">&quot;</span><span style="color:#A6ACCD;">dynamicId</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">&gt;&lt;/</span><span style="color:#F07178;">div</span><span style="color:#89DDFF;">&gt;</span></span>\n<span class="line"></span></code></pre></div><p>Attributes that start with <code>:</code> may look a bit different from normal HTML, but it is in fact a valid character for attribute names and all Vue-supported browsers can parse it correctly. In addition, they do not appear in the final rendered markup. The shorthand syntax is optional, but you will likely appreciate it when you learn more about its usage later.</p><p><code>:</code>로 시작하는 속성은 일반 HTML과 약간 다르게 보일 수 있지만 실제로는 HTML 속성명으로 유효한 문자이며 Vue를 지원하는 모든 브라우저에서 올바르게 파싱할수 있습니다. 또한 최종 렌더링된 마크업에는 표시되지 않습니다. 약식 구문은 선택 사항이지만 나중에 사용법에 대해 자세히 알아볼 때 유용할 것입니다.</p><blockquote><p>For the rest of the guide, we will be using the shorthand syntax in code examples, as that&#39;s the most common usage for Vue developers.</p></blockquote><blockquote><p>이후 가이드 문서에서는 단축 구문을 사용해서 예제를 설명하겠습니다.</p></blockquote><h3 id="boolean-attributes" tabindex="-1">Boolean Attributes <a class="header-anchor" href="#boolean-attributes" aria-hidden="true">#</a></h3><h3 id="boolean-속성" tabindex="-1">Boolean 속성 <a class="header-anchor" href="#boolean-속성" aria-hidden="true">#</a></h3><p><a href="https://html.spec.whatwg.org/multipage/common-microsyntaxes.html#boolean-attributes" target="_blank" rel="noopener noreferrer">Boolean attributes</a> are attributes that can indicate true / false values by its presence on an element. For example, <a href="https://developer.mozilla.org/en-US/docs/Web/HTML/Attributes/disabled" target="_blank" rel="noopener noreferrer"><code>disabled</code></a> is one of the most commonly used boolean attributes.</p><p><a href="https://html.spec.whatwg.org/multipage/common-microsyntaxes.html#boolean-attributes" target="_blank" rel="noopener noreferrer">Boolean 속성</a>은 참/거짓 값을 이용해 엘리먼트 속성 자체의 표시 여부를 나타낼수 있습니다. 예를 들어, <a href="https://developer.mozilla.org/en-US/docs/Web/HTML/Attributes/disabled" target="_blank" rel="noopener noreferrer"><code>disabled</code></a>는 가장 일반적으로 사용되는 Boolean 속성 중 하나입니다.</p><p><code>v-bind</code> works a bit differently in this case:</p><p><code>v-bind</code>는 이 경우 약간 다르게 작동합니다:</p><div class="language-vue-html"><pre><code><span class="line"><span style="color:#89DDFF;">&lt;</span><span style="color:#F07178;">button</span><span style="color:#89DDFF;"> :</span><span style="color:#C792EA;">disabled</span><span style="color:#89DDFF;">=</span><span style="color:#89DDFF;">&quot;</span><span style="color:#A6ACCD;">isButtonDisabled</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">&gt;</span><span style="color:#A6ACCD;">Button</span><span style="color:#89DDFF;">&lt;/</span><span style="color:#F07178;">button</span><span style="color:#89DDFF;">&gt;</span></span>\n<span class="line"></span></code></pre></div><p>The <code>disabled</code> attribute will be included if <code>isButtonDisabled</code> has a <a href="https://developer.mozilla.org/en-US/docs/Glossary/Truthy" target="_blank" rel="noopener noreferrer">truthy value</a>. It will also be included if the value is an empty string, maintaining consistency with <code>&lt;button disabled=&quot;&quot;&gt;</code>. For other falsy values the attribute will be omitted.</p><p><code>isButtonDisabled</code>에 <a href="https://developer.mozilla.org/en-US/docs/Glossary/Truthy" target="_blank" rel="noopener noreferrer">참(truthy) 값</a>이 있는 경우 <code>disabled</code> 속성이 포함됩니다. 값이 빈 문자열인 경우에도 포함되어 <code>&lt;button disabled=&quot;&quot;&gt;</code>와 일관성을 유지합니다. 거짓 값의 경우 속성이 생략됩니다.</p><h3 id="dynamically-binding-multiple-attributes" tabindex="-1">Dynamically Binding Multiple Attributes <a class="header-anchor" href="#dynamically-binding-multiple-attributes" aria-hidden="true">#</a></h3><h3 id="여러-속성에-대해-동적인-바인딩" tabindex="-1">여러 속성에 대해 동적인 바인딩 <a class="header-anchor" href="#여러-속성에-대해-동적인-바인딩" aria-hidden="true">#</a></h3><p>If you have a JavaScript object representing multiple attributes that looks like this:</p><p>다음과 같은 여러 속성을 나타내는 JavaScript 객체가 있는 경우:</p><div class="composition-api"><div class="language-js"><pre><code><span class="line"><span style="color:#C792EA;">const</span><span style="color:#A6ACCD;"> objectOfAttrs </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">{</span></span>\n<span class="line"><span style="color:#A6ACCD;">  </span><span style="color:#F07178;">id</span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">container</span><span style="color:#89DDFF;">&#39;</span><span style="color:#89DDFF;">,</span></span>\n<span class="line"><span style="color:#A6ACCD;">  </span><span style="color:#F07178;">class</span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">wrapper</span><span style="color:#89DDFF;">&#39;</span></span>\n<span class="line"><span style="color:#89DDFF;">}</span></span>\n<span class="line"></span></code></pre></div></div><div class="options-api"><div class="language-js"><pre><code><span class="line"><span style="color:#82AAFF;">data</span><span style="color:#A6ACCD;">() </span><span style="color:#89DDFF;">{</span></span>\n<span class="line"><span style="color:#F07178;">  </span><span style="color:#89DDFF;font-style:italic;">return</span><span style="color:#F07178;"> </span><span style="color:#89DDFF;">{</span></span>\n<span class="line"><span style="color:#F07178;">    objectOfAttrs</span><span style="color:#89DDFF;">:</span><span style="color:#F07178;"> </span><span style="color:#89DDFF;">{</span></span>\n<span class="line"><span style="color:#F07178;">      id</span><span style="color:#89DDFF;">:</span><span style="color:#F07178;"> </span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">container</span><span style="color:#89DDFF;">&#39;</span><span style="color:#89DDFF;">,</span></span>\n<span class="line"><span style="color:#F07178;">      class</span><span style="color:#89DDFF;">:</span><span style="color:#F07178;"> </span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">wrapper</span><span style="color:#89DDFF;">&#39;</span></span>\n<span class="line"><span style="color:#F07178;">    </span><span style="color:#89DDFF;">}</span></span>\n<span class="line"><span style="color:#F07178;">  </span><span style="color:#89DDFF;">}</span></span>\n<span class="line"><span style="color:#89DDFF;">}</span></span>\n<span class="line"></span></code></pre></div></div><p>You can bind them to a single element by using <code>v-bind</code> without an argument:</p><p>인자 지정 없이 <code>v-bind</code>를 사용하여 단일 엘리먼트에 바인딩할 수 있습니다:</p><div class="language-vue-html"><pre><code><span class="line"><span style="color:#89DDFF;">&lt;</span><span style="color:#F07178;">div</span><span style="color:#89DDFF;"> </span><span style="color:#C792EA;">v-bind</span><span style="color:#89DDFF;">=</span><span style="color:#89DDFF;">&quot;</span><span style="color:#A6ACCD;">objectOfAttrs</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">&gt;&lt;/</span><span style="color:#F07178;">div</span><span style="color:#89DDFF;">&gt;</span></span>\n<span class="line"></span></code></pre></div><h2 id="using-javascript-expressions" tabindex="-1">Using JavaScript Expressions <a class="header-anchor" href="#using-javascript-expressions" aria-hidden="true">#</a></h2><h2 id="자바스크립트-표현식-사용하기" tabindex="-1">자바스크립트 표현식 사용하기 <a class="header-anchor" href="#자바스크립트-표현식-사용하기" aria-hidden="true">#</a></h2><p>So far we&#39;ve only been binding to simple property keys in our templates. But Vue actually supports the full power of JavaScript expressions inside all data bindings:</p><p>지금까지는 템플릿의 간단한 속성 키로만 바인딩했습니다. 그러나 Vue는 실제로 모든 데이터 바인딩 내에서 JavaScript 표현식의 모든 기능을 지원합니다:</p><div class="language-vue-html"><pre><code><span class="line"><span style="color:#89DDFF;">{{</span><span style="color:#A6ACCD;"> number </span><span style="color:#89DDFF;">+</span><span style="color:#A6ACCD;"> </span><span style="color:#F78C6C;">1</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">}}</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#89DDFF;">{{</span><span style="color:#A6ACCD;"> ok </span><span style="color:#89DDFF;">?</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">YES</span><span style="color:#89DDFF;">&#39;</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">NO</span><span style="color:#89DDFF;">&#39;</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">}}</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#89DDFF;">{{</span><span style="color:#A6ACCD;"> message</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">split</span><span style="color:#A6ACCD;">(</span><span style="color:#89DDFF;">&#39;&#39;</span><span style="color:#A6ACCD;">)</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">reverse</span><span style="color:#A6ACCD;">()</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">join</span><span style="color:#A6ACCD;">(</span><span style="color:#89DDFF;">&#39;&#39;</span><span style="color:#A6ACCD;">) </span><span style="color:#89DDFF;">}}</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#89DDFF;">&lt;</span><span style="color:#F07178;">div</span><span style="color:#89DDFF;"> :</span><span style="color:#C792EA;">id</span><span style="color:#89DDFF;">=</span><span style="color:#89DDFF;">&quot;`</span><span style="color:#C3E88D;">list-</span><span style="color:#89DDFF;">${</span><span style="color:#A6ACCD;">id</span><span style="color:#89DDFF;">}`&quot;</span><span style="color:#89DDFF;">&gt;&lt;/</span><span style="color:#F07178;">div</span><span style="color:#89DDFF;">&gt;</span></span>\n<span class="line"></span></code></pre></div><p>These expressions will be evaluated as JavaScript in the data scope of the current component instance.</p><p>이러한 표현식은 현재 컴포넌트 인스턴스가 가진 데이터를 이용해 JavaScript로 평가됩니다.</p><p>In Vue templates, JavaScript expressions can be used in the following positions:</p><p>Vue 템플릿에서 JavaScript 표현식은 다음 위치에서 사용할 수 있습니다:</p><ul><li>Inside text interpolations (mustaches)</li><li>문자열 보간 내에서 (mustaches)</li><li>In the attribute value of any Vue directives (special attributes that start with <code>v-</code>)</li><li>Vue 디렉티트 속성 값 안에서 (<code>v-</code>로 시작하는 특수 속성)</li></ul><h3 id="expressions-only" tabindex="-1">Expressions Only <a class="header-anchor" href="#expressions-only" aria-hidden="true">#</a></h3><h3 id="표현식만-사용가능" tabindex="-1">표현식만 사용가능 <a class="header-anchor" href="#표현식만-사용가능" aria-hidden="true">#</a></h3><p>Each binding can only contain <strong>one single expression</strong>, so the following will <strong>NOT</strong> work:</p><p>각 바인딩은 <strong>하나의 단일 표현식</strong>만 포함할 수 있으므로 다음은 작동하지 <strong>않습니다</strong>:</p><div class="language-vue-html"><pre><code><span class="line"><span style="color:#676E95;font-style:italic;">&lt;!-- 이건 표현식(expression)이 아니라 문장(statement)임: --&gt;</span></span>\n<span class="line"><span style="color:#89DDFF;">{{</span><span style="color:#A6ACCD;"> var a </span><span style="color:#89DDFF;">=</span><span style="color:#A6ACCD;"> </span><span style="color:#F78C6C;">1</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">}}</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#676E95;font-style:italic;">&lt;!-- 흐름 제어역시 동작하지 않음. 3항연산자( `condition ? a : b`)를 사용하세요:   --&gt;</span></span>\n<span class="line"><span style="color:#89DDFF;">{{</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">if</span><span style="color:#A6ACCD;"> (ok) </span><span style="color:#89DDFF;">{</span><span style="color:#A6ACCD;"> return message </span><span style="color:#89DDFF;">}</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">}}</span></span>\n<span class="line"></span></code></pre></div><h3 id="calling-functions" tabindex="-1">Calling Functions <a class="header-anchor" href="#calling-functions" aria-hidden="true">#</a></h3><h3 id="함수-호출하기" tabindex="-1">함수 호출하기 <a class="header-anchor" href="#함수-호출하기" aria-hidden="true">#</a></h3><p>It is possible to call a component-exposed method inside a binding expression:</p><p>바인딩 식 내에서 컴포넌트에서 제공하는 메서드를 호출할 수 있습니다:</p><div class="language-vue-html"><pre><code><span class="line"><span style="color:#89DDFF;">&lt;</span><span style="color:#F07178;">span</span><span style="color:#89DDFF;"> :</span><span style="color:#C792EA;">title</span><span style="color:#89DDFF;">=</span><span style="color:#89DDFF;">&quot;</span><span style="color:#82AAFF;">toTitleDate</span><span style="color:#89DDFF;">(</span><span style="color:#A6ACCD;">date</span><span style="color:#89DDFF;">)</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">&gt;</span></span>\n<span class="line"><span style="color:#A6ACCD;">  </span><span style="color:#89DDFF;">{{</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">formatDate</span><span style="color:#A6ACCD;">(date) </span><span style="color:#89DDFF;">}}</span></span>\n<span class="line"><span style="color:#89DDFF;">&lt;/</span><span style="color:#F07178;">span</span><span style="color:#89DDFF;">&gt;</span></span>\n<span class="line"></span></code></pre></div><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>Functions called inside binding expressions will be called every time the component updates, so they should <strong>not</strong> have any side effects, such as changing data or triggering asynchronous operations.</p></div><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>바인딩 표현식 내에서 호출되는 함수는 컴포넌트가 업데이트될 때마다 호출되므로 데이터 변경 또는 비동기 작업 트리거와 같은 부작용이 <strong>없어야 합니다</strong>.</p></div><h3 id="restricted-globals-access" tabindex="-1">Restricted Globals Access <a class="header-anchor" href="#restricted-globals-access" aria-hidden="true">#</a></h3><h3 id="전역-객체-접근-제한" tabindex="-1">전역 객체 접근 제한 <a class="header-anchor" href="#전역-객체-접근-제한" aria-hidden="true">#</a></h3><p>Template expressions are sandboxed and only have access to a <a href="https://github.com/vuejs/core/blob/main/packages/shared/src/globalsWhitelist.ts#L3" target="_blank" rel="noopener noreferrer">restricted list of globals</a>. The list exposes commonly used built-in globals such as <code>Math</code> and <code>Date</code>.</p><p>템플릿 표현식은 샌드박스 처리되며 <a href="https://github.com/vuejs/core/blob/main/packages/shared/src/globalsWhitelist.ts#L3" target="_blank" rel="noopener noreferrer">제한된 전역 객체 목록</a>에만 액세스할 수 있습니다. 이 목록은 <code>Math</code> 및 <code>Date</code>와 같이 일반적으로 사용되는 내장 전역 객체를 표시합니다.</p><p>Globals not explicitly included in the list, for example user-attached properties on <code>window</code>, will not be accessible in template expressions. You can, however, explicitly define additional globals for all Vue expressions by adding them to <a href="/api/application.html#app-config-globalproperties"><code>app.config.globalProperties</code></a>.</p><p>목록에 명시적으로 포함되지 않은 전역 객체(예: <code>window</code>의 사용자 연결 속성)는 템플릿 표현식에서 액세스할 수 없습니다. 그러나 <a href="/api/application.html#app-config-globalproperties"><code>app.config.globalProperties</code></a>에 추가하여 모든 Vue 표현식에 대한 추가 전역 객체를 명시적으로 정의할 수 있습니다.</p><h2 id="directives" tabindex="-1">Directives <a class="header-anchor" href="#directives" aria-hidden="true">#</a></h2><h2 id="디렉티브" tabindex="-1">디렉티브 <a class="header-anchor" href="#디렉티브" aria-hidden="true">#</a></h2><p>Directives are special attributes with the <code>v-</code> prefix. Vue provides a number of <a href="/api/built-in-directives.html">built-in directives</a>, including <code>v-html</code> and <code>v-bind</code> which we have introduced above.</p><p>디렉티브은 <code>v-</code> 접두사가 있는 특수 속성입니다. Vue는 위에서 소개한 <code>v-html</code> 및 <code>v-bind</code>를 포함하여 다양한 <a href="/api/built-in-directives.html">내장 디렉티브</a>을 제공합니다.</p><p>Directive attribute values are expected to be single JavaScript expressions (with the exception of <code>v-for</code>, <code>v-on</code> and <code>v-slot</code>, which will be discussed in their respective sections later). A directive&#39;s job is to reactively apply updates to the DOM when the value of its expression changes. Take <a href="/api/built-in-directives.html#v-if"><code>v-if</code></a> as an example:</p><p>디렉티브 속성 값은 단일 JavaScript 표현식이어야 합니다(나중에 해당 섹션에서 논의할 <code>v-for</code>, <code>v-on</code> 및 <code>v-slot</code> 제외). 디렉티브의 역할은 표현식 값이 변경될 때 DOM에 반응적으로 업데이트를 적용하는 것입니다. <a href="/api/built-in-directives.html#v-if"><code>v-if</code></a>를 예로 들어 보겠습니다.</p><div class="language-vue-html"><pre><code><span class="line"><span style="color:#89DDFF;">&lt;</span><span style="color:#F07178;">p</span><span style="color:#89DDFF;"> </span><span style="color:#C792EA;">v-if</span><span style="color:#89DDFF;">=</span><span style="color:#89DDFF;">&quot;</span><span style="color:#A6ACCD;">seen</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">&gt;</span><span style="color:#A6ACCD;">Now you see me</span><span style="color:#89DDFF;">&lt;/</span><span style="color:#F07178;">p</span><span style="color:#89DDFF;">&gt;</span></span>\n<span class="line"></span></code></pre></div><p>Here, the <code>v-if</code> directive would remove / insert the <code>&lt;p&gt;</code> element based on the truthiness of the value of the expression <code>seen</code>.</p><p>여기서 <code>v-if</code> 디렉티브는 <code>seen</code> 표현 값의 진실성을 기반으로 <code>&lt;p&gt;</code>엘리먼트를 제거/삽입합니다.</p><h3 id="arguments" tabindex="-1">Arguments <a class="header-anchor" href="#arguments" aria-hidden="true">#</a></h3><h3 id="인자" tabindex="-1">인자 <a class="header-anchor" href="#인자" aria-hidden="true">#</a></h3><p>Some directives can take an &quot;argument&quot;, denoted by a colon after the directive name. For example, the <code>v-bind</code> directive is used to reactively update an HTML attribute:</p><p>일부 디렉티브는 디렉티브 이름 뒤에 콜론으로 표시되는 &quot;인자(argument)&quot;를 사용할 수 있습니다. 예를 들어 <code>v-bind</code> 디렉티브는 HTML 속성을 반응적으로 업데이트하는 데 사용됩니다:</p><div class="language-vue-html"><pre><code><span class="line"><span style="color:#89DDFF;">&lt;</span><span style="color:#F07178;">a</span><span style="color:#89DDFF;"> </span><span style="color:#C792EA;">v-bind</span><span style="color:#89DDFF;">:</span><span style="color:#C792EA;">href</span><span style="color:#89DDFF;">=</span><span style="color:#89DDFF;">&quot;</span><span style="color:#A6ACCD;">url</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">&gt;</span><span style="color:#A6ACCD;"> ... </span><span style="color:#89DDFF;">&lt;/</span><span style="color:#F07178;">a</span><span style="color:#89DDFF;">&gt;</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#676E95;font-style:italic;">&lt;!-- shorthand --&gt;</span></span>\n<span class="line"><span style="color:#89DDFF;">&lt;</span><span style="color:#F07178;">a</span><span style="color:#89DDFF;"> :</span><span style="color:#C792EA;">href</span><span style="color:#89DDFF;">=</span><span style="color:#89DDFF;">&quot;</span><span style="color:#A6ACCD;">url</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">&gt;</span><span style="color:#A6ACCD;"> ... </span><span style="color:#89DDFF;">&lt;/</span><span style="color:#F07178;">a</span><span style="color:#89DDFF;">&gt;</span></span>\n<span class="line"></span></code></pre></div><p>Here <code>href</code> is the argument, which tells the <code>v-bind</code> directive to bind the element&#39;s <code>href</code> attribute to the value of the expression <code>url</code>. In the shorthand, everything before the argument (i.e. <code>v-bind:</code>) is condensed into a single character, <code>:</code>.</p><p>여기서 <code>href</code>는엘리먼트의 <code>href</code> 속성을 표현식 <code>url</code>의 값에 바인딩하도록 <code>v-bind</code> 디렉티브에 지시하는 인자입니다. 간단히 말해서, 인자 앞의 모든 것(즉, <code>v-bind:</code>)은 단일 문자 <code>:</code>로 압축됩니다.</p><p>Another example is the <code>v-on</code> directive, which listens to DOM events:</p><p>또 다른 예는 DOM 이벤트를 수신하는 <code>v-on</code> 디렉티브입니다:</p><div class="language-vue-html"><pre><code><span class="line"><span style="color:#89DDFF;">&lt;</span><span style="color:#F07178;">a</span><span style="color:#89DDFF;"> </span><span style="color:#C792EA;">v-on</span><span style="color:#89DDFF;">:</span><span style="color:#C792EA;">click</span><span style="color:#89DDFF;">=</span><span style="color:#89DDFF;">&quot;</span><span style="color:#A6ACCD;">doSomething</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">&gt;</span><span style="color:#A6ACCD;"> ... </span><span style="color:#89DDFF;">&lt;/</span><span style="color:#F07178;">a</span><span style="color:#89DDFF;">&gt;</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#676E95;font-style:italic;">&lt;!-- shorthand --&gt;</span></span>\n<span class="line"><span style="color:#89DDFF;">&lt;</span><span style="color:#F07178;">a</span><span style="color:#89DDFF;"> @</span><span style="color:#C792EA;">click</span><span style="color:#89DDFF;">=</span><span style="color:#89DDFF;">&quot;</span><span style="color:#A6ACCD;">doSomething</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">&gt;</span><span style="color:#A6ACCD;"> ... </span><span style="color:#89DDFF;">&lt;/</span><span style="color:#F07178;">a</span><span style="color:#89DDFF;">&gt;</span></span>\n<span class="line"></span></code></pre></div><p>Here the argument is the event name to listen to: <code>click</code>. <code>v-on</code> is one of the few directives that also have a corresponding shorthand, with its shorthand character being <code>@</code>. We will talk about event handling in more detail too.</p><p>여기 인자는 수신할 이벤트 이름입니다. <code>click</code>. <code>v-on</code>은 전용 약어가 있는 몇 안 되는 디렉티브 중 하나이며 약어 문자는 <code>@</code>입니다. 이벤트 처리에 대해서도 더 자세히 이야기하겠습니다.</p><h3 id="dynamic-arguments" tabindex="-1">Dynamic Arguments <a class="header-anchor" href="#dynamic-arguments" aria-hidden="true">#</a></h3><h3 id="동적-인자-지정" tabindex="-1">동적 인자 지정 <a class="header-anchor" href="#동적-인자-지정" aria-hidden="true">#</a></h3><p>It is also possible to use a JavaScript expression in a directive argument by wrapping it with square brackets:</p><p>대괄호로 감싸서 디렉티브 인자에서 JavaScript 표현식을 사용할 수도 있습니다:</p><div class="language-vue-html"><pre><code><span class="line"><span style="color:#676E95;font-style:italic;">&lt;!--</span></span>\n<span class="line"><span style="color:#676E95;font-style:italic;">Note that there are some constraints to the argument expression,</span></span>\n<span class="line"><span style="color:#676E95;font-style:italic;">as explained in the &quot;Dynamic Argument Value Constraints&quot; and &quot;Dynamic Argument Syntax Constraints&quot; sections below.</span></span>\n<span class="line"><span style="color:#676E95;font-style:italic;">--&gt;</span></span>\n<span class="line"><span style="color:#89DDFF;">&lt;</span><span style="color:#F07178;">a</span><span style="color:#89DDFF;"> </span><span style="color:#C792EA;">v-bind:</span><span style="color:#89DDFF;">[</span><span style="color:#C792EA;">attributeName</span><span style="color:#89DDFF;">]=</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">url</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">&gt;</span><span style="color:#A6ACCD;"> ... </span><span style="color:#89DDFF;">&lt;/</span><span style="color:#F07178;">a</span><span style="color:#89DDFF;">&gt;</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#676E95;font-style:italic;">&lt;!-- shorthand --&gt;</span></span>\n<span class="line"><span style="color:#89DDFF;">&lt;</span><span style="color:#F07178;">a</span><span style="color:#89DDFF;"> :[</span><span style="color:#C792EA;">attributeName</span><span style="color:#89DDFF;">]=</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">url</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">&gt;</span><span style="color:#A6ACCD;"> ... </span><span style="color:#89DDFF;">&lt;/</span><span style="color:#F07178;">a</span><span style="color:#89DDFF;">&gt;</span></span>\n<span class="line"></span></code></pre></div><p>Here <code>attributeName</code> will be dynamically evaluated as a JavaScript expression, and its evaluated value will be used as the final value for the argument. For example, if your component instance has a data property, <code>attributeName</code>, whose value is <code>&quot;href&quot;</code>, then this binding will be equivalent to <code>v-bind:href</code>.</p><p>여기서 <code>attributeName</code>은 JavaScript 표현식으로 동적으로 평가되며 평가된 값은 인자의 최종 값으로 사용됩니다. 예를 들어 컴포넌트 인스턴스에 값이 <code>&quot;href&quot;</code>인 데이터 속성 <code>attributeName</code>이 있는 경우 이 바인딩은 <code>v-bind:href</code>와 동일합니다.</p><p>Similarly, you can use dynamic arguments to bind a handler to a dynamic event name:</p><p>마찬가지로 동적 인자를 사용하여 핸들러를 동적 이벤트 이름에 바인딩할 수 있습니다:</p><div class="language-vue-html"><pre><code><span class="line"><span style="color:#89DDFF;">&lt;</span><span style="color:#F07178;">a</span><span style="color:#89DDFF;"> </span><span style="color:#C792EA;">v-on:</span><span style="color:#89DDFF;">[</span><span style="color:#C792EA;">eventName</span><span style="color:#89DDFF;">]=</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">doSomething</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">&gt;</span><span style="color:#A6ACCD;"> ... </span><span style="color:#89DDFF;">&lt;/</span><span style="color:#F07178;">a</span><span style="color:#89DDFF;">&gt;</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#676E95;font-style:italic;">&lt;!-- shorthand --&gt;</span></span>\n<span class="line"><span style="color:#89DDFF;">&lt;</span><span style="color:#F07178;">a</span><span style="color:#89DDFF;"> @[</span><span style="color:#C792EA;">eventName</span><span style="color:#89DDFF;">]=</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">doSomething</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">&gt;</span></span>\n<span class="line"></span></code></pre></div><p>In this example, when <code>eventName</code>&#39;s value is <code>&quot;focus&quot;</code>, <code>v-on:[eventName]</code> will be equivalent to <code>v-on:focus</code>.</p><p>이 예에서 <code>eventName</code>의 값이 <code>&quot;focus&quot;</code>인 경우 <code>v-on:[eventName]</code>은 <code>v-on:focus</code>와 동일합니다.</p><h4 id="dynamic-argument-value-constraints" tabindex="-1">Dynamic Argument Value Constraints <a class="header-anchor" href="#dynamic-argument-value-constraints" aria-hidden="true">#</a></h4><h4 id="동적-인자-값-제한" tabindex="-1">동적 인자 값 제한 <a class="header-anchor" href="#동적-인자-값-제한" aria-hidden="true">#</a></h4><p>Dynamic arguments are expected to evaluate to a string, with the exception of <code>null</code>. The special value <code>null</code> can be used to explicitly remove the binding. Any other non-string value will trigger a warning.</p><p>동적 인자는 <code>null</code>을 제외하고 문자열로 평가되어야 합니다. 특수 값 <code>null</code>을 사용하여 바인딩을 명시적으로 제거할 수 있습니다. 문자열이 아닌 다른 값은 경고를 날립니다.</p><h4 id="dynamic-argument-syntax-constraints" tabindex="-1">Dynamic Argument Syntax Constraints <a class="header-anchor" href="#dynamic-argument-syntax-constraints" aria-hidden="true">#</a></h4><h4 id="동적-인자-문법-제한" tabindex="-1">동적 인자 문법 제한 <a class="header-anchor" href="#동적-인자-문법-제한" aria-hidden="true">#</a></h4><p>Dynamic argument expressions have some syntax constraints because certain characters, such as spaces and quotes, are invalid inside HTML attribute names. For example, the following is invalid:</p><p>동적 인자 표현식에는 공백 및 따옴표와 같은 특정 문자가 HTML 속성 이름 내에서 유효하지 않기 때문에 일부 구문 제약 조건이 있습니다. 예를 들어 다음은 유효하지 않습니다:</p><div class="language-vue-html"><pre><code><span class="line"><span style="color:#676E95;font-style:italic;">&lt;!-- This will trigger a compiler warning. --&gt;</span></span>\n<span class="line"><span style="color:#89DDFF;">&lt;</span><span style="color:#F07178;">a</span><span style="color:#89DDFF;"> :[</span><span style="color:#89DDFF;">&#39;</span><span style="color:#C3E88D;">foo</span><span style="color:#89DDFF;">&#39;</span><span style="color:#89DDFF;"> + </span><span style="color:#C792EA;">bar</span><span style="color:#89DDFF;">]=</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">value</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">&gt;</span><span style="color:#A6ACCD;"> ... </span><span style="color:#89DDFF;">&lt;/</span><span style="color:#F07178;">a</span><span style="color:#89DDFF;">&gt;</span></span>\n<span class="line"></span></code></pre></div><p>If you need to pass a complex dynamic argument, it&#39;s probably better to use a <a href="./computed.html">computed property</a>, which we will cover shortly.</p><p>복잡한 동적 인자를 전달해야 하는 경우 <a href="./computed.html">computed property</a>를 사용하는 것이 더 나을 것입니다. 이에 대해서는 곧 다룰 것입니다.</p><p>When using in-DOM templates (templates directly written in an HTML file), you should also avoid naming keys with uppercase characters, as browsers will coerce attribute names into lowercase:</p><p>DOM 내 템플릿(HTML 파일에 직접 작성된 템플릿)을 사용할 때 브라우저가 속성 이름을 소문자로 강제 변환하므로 대문자로 키 이름을 지정하는 것도 피해야 합니다:</p><div class="language-vue-html"><pre><code><span class="line"><span style="color:#89DDFF;">&lt;</span><span style="color:#F07178;">a</span><span style="color:#89DDFF;"> :[</span><span style="color:#C792EA;">someAttr</span><span style="color:#89DDFF;">]=</span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">value</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">&gt;</span><span style="color:#A6ACCD;"> ... </span><span style="color:#89DDFF;">&lt;/</span><span style="color:#F07178;">a</span><span style="color:#89DDFF;">&gt;</span></span>\n<span class="line"></span></code></pre></div><p>The above will be converted to <code>:[someattr]</code> in in-DOM templates. If your component has a <code>someAttr</code> property instead of <code>someattr</code>, your code won&#39;t work.</p><p>위의 내용은 DOM 내 템플릿에서 <code>:[someattr]</code>로 변환됩니다. 컴포넌트에 <code>someattr</code> 대신 <code>someAttr</code> 속성이 있으면 코드가 작동하지 않습니다.</p><h3 id="modifiers" tabindex="-1">Modifiers <a class="header-anchor" href="#modifiers" aria-hidden="true">#</a></h3><h3 id="수정자-modifiers" tabindex="-1">수정자(Modifiers) <a class="header-anchor" href="#수정자-modifiers" aria-hidden="true">#</a></h3><p>Modifiers are special postfixes denoted by a dot, which indicate that a directive should be bound in some special way. For example, the <code>.prevent</code> modifier tells the <code>v-on</code> directive to call <code>event.preventDefault()</code> on the triggered event:</p><p>수정자는 점으로 표시되는 특수 접미사로 디렉티브이 특별한 방식으로 바인딩되어야 함을 나타냅니다. 예를 들어 <code>.prevent</code> 수정자는 트리거된 이벤트에서 <code>event.preventDefault()</code>를 호출하도록 <code>v-on</code> 디렉티브에 지시합니다.</p><div class="language-vue-html"><pre><code><span class="line"><span style="color:#89DDFF;">&lt;</span><span style="color:#F07178;">form</span><span style="color:#89DDFF;"> @</span><span style="color:#C792EA;">submit</span><span style="color:#89DDFF;">.</span><span style="color:#C792EA;">prevent</span><span style="color:#89DDFF;">=</span><span style="color:#89DDFF;">&quot;</span><span style="color:#A6ACCD;">onSubmit</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">&gt;</span><span style="color:#A6ACCD;">...</span><span style="color:#89DDFF;">&lt;/</span><span style="color:#F07178;">form</span><span style="color:#89DDFF;">&gt;</span></span>\n<span class="line"></span></code></pre></div><p>You&#39;ll see other examples of modifiers later, <a href="./event-handling.html#event-modifiers">for <code>v-on</code></a> and <a href="./forms.html#modifiers">for <code>v-model</code></a>, when we explore those features.</p><p>나중에 <a href="./event-handling.html#event-modifiers"><code>v-on</code></a> 및 <a href="./forms.html#modifiers"><code>v-model</code></a> 기능을을 다룰때 해당 기능들의 수정자들에 대한 예시들을 보실수 있습니다.</p><p>And finally, here&#39;s the full directive syntax visualized:</p><p>마지막으로 다음은 전체 디렉티브 문법을 시각화한 것입니다.</p><p><img src="/assets/directive.69c37117.png" alt="directive syntax graph"></p>',127),c='{"title":"Template Syntax","description":"","frontmatter":{},"headers":[{"level":2,"title":"Text Interpolation","slug":"text-interpolation"},{"level":2,"title":"텍스트 보간(Text Interpolation)","slug":"텍스트-보간-text-interpolation"},{"level":2,"title":"Raw HTML","slug":"raw-html"},{"level":2,"title":"Attribute Bindings","slug":"attribute-bindings"},{"level":2,"title":"속성 바인딩","slug":"속성-바인딩"},{"level":3,"title":"Shorthand","slug":"shorthand"},{"level":3,"title":"단축 표기법","slug":"단축-표기법"},{"level":3,"title":"Boolean Attributes","slug":"boolean-attributes"},{"level":3,"title":"Boolean 속성","slug":"boolean-속성"},{"level":3,"title":"Dynamically Binding Multiple Attributes","slug":"dynamically-binding-multiple-attributes"},{"level":3,"title":"여러 속성에 대해 동적인 바인딩","slug":"여러-속성에-대해-동적인-바인딩"},{"level":2,"title":"Using JavaScript Expressions","slug":"using-javascript-expressions"},{"level":2,"title":"자바스크립트 표현식 사용하기","slug":"자바스크립트-표현식-사용하기"},{"level":3,"title":"Expressions Only","slug":"expressions-only"},{"level":3,"title":"표현식만 사용가능","slug":"표현식만-사용가능"},{"level":3,"title":"Calling Functions","slug":"calling-functions"},{"level":3,"title":"함수 호출하기","slug":"함수-호출하기"},{"level":3,"title":"Restricted Globals Access","slug":"restricted-globals-access"},{"level":3,"title":"전역 객체 접근 제한","slug":"전역-객체-접근-제한"},{"level":2,"title":"Directives","slug":"directives"},{"level":2,"title":"디렉티브","slug":"디렉티브"},{"level":3,"title":"Arguments","slug":"arguments"},{"level":3,"title":"인자","slug":"인자"},{"level":3,"title":"Dynamic Arguments","slug":"dynamic-arguments"},{"level":3,"title":"동적 인자 지정","slug":"동적-인자-지정"},{"level":3,"title":"Modifiers","slug":"modifiers"},{"level":3,"title":"수정자(Modifiers)","slug":"수정자-modifiers"}],"relativePath":"guide/essentials/template-syntax.md"}',i={},d=Object.assign(i,{setup(n){const o='<span style="color: red">This should be red.</span>';return(n,c)=>(t(),e("div",null,[l,s("div",{class:"demo"},[s("p",null,"문자열 보간: "+a(o)),s("p",null,[p,s("span",{innerHTML:o})])]),r]))}});export{c as __pageData,d as default};
